package Functional_Interface;
// for defination run this file
public class Defination {
    void getFunctionalInterface(){
        System.out.println(" Defintion : Interface having exactly single method \n - can have any number of default and static methods \n - We can invoke lambda expression using functional interface." );
        System.out.println("Special Note : 1.7 tak only public abstract methods are allowed inside interface from 1.8 default and static methods are allowed ");
    }
}
